package org.tribosmap.frontend.map

/**
 * Communication codes for the MapActivities (MapEdityActivity, MapViewActivity)
 * @author Meiko Rachimow
 */
protected[map] object MapComCodes {
  
  /**
   * this flag is used as a key to identify a serialized GeoMap-object in a bundle
   */
  val MAP_PARAMETER_ID = "MAP_PARAMETER_ID"
  
}
